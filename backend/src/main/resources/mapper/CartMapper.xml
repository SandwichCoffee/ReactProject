<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.portfolio.backend.mapper.CartMapper">

    <select id="selectCartList" parameterType="String" resultType="com.portfolio.backend.vo.CartVO">
        SELECT c.cart_id,
        c.user_id,
        c.prod_id,
        c.quantity,
        p.prod_name,
        p.prod_price,
        p.prod_img
        FROM tb_cart c
        JOIN tb_product p ON c.prod_id = p.prod_id
        WHERE c.user_id = #{userId}
        ORDER BY c.cart_id DESC
    </select>

    <insert id="addCart" parameterType="com.portfolio.backend.vo.CartVO">
        INSERT INTO tb_cart (user_id, prod_id, quantity)
        VALUES (#{userId}, #{prodId}, #{quantity})
        ON DUPLICATE KEY UPDATE quantity = quantity + #{quantity}
    </insert>

    <update id="updateQuantity" parameterType="com.portfolio.backend.vo.CartVO">
        UPDATE tb_cart
        SET quantity = #{quantity}
        WHERE cart_id = #{cartId}
    </update>

    <delete id="deleteCart" parameterType="int">
        DELETE FROM tb_cart WHERE cart_id = #{cartId}
    </delete>

</mapper>